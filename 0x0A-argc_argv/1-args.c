#include <stdio.h>
#include "main.h"
/**
 * main - Entry point,
 * @argc: argument
 * @argv: argument
 * Return: Always 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
