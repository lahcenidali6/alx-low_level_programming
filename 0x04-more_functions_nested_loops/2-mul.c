#include "main.h"
/**
 * mul - function that checks for a digit
 *
 * @a: the firs number
 *
 *@b: the second number
 *
 * Return: multiplies of a and b
 */
int mul(int a, int b)
{
	return ((a * b));
}
