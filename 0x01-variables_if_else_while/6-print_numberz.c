#include <stdio.h>
/**
 * main - Entry point, print Programming is like building a multilingual puzzle
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        int i = 49;

        while (i < 58)
        {
                putchar(i);
                i++;
        }
        putchar('\n');
        return (0);
}
